import './App.css';
import Header from './Header'

function App() {
  return (
    // We will use the BEM convention for naming
    <div className="app">
      <h1>Hello Programmers, Let's build the Amazon store app </h1>
      <Header />
      {/* Header */}
      {/* Home */}
    </div>
  );
}

export default App;
